# 前端联调测试指南

## 一、环境准备

### 1. 确认数据库已启动
确保 MySQL 服务正在运行，数据库 `Hogwarts Houses` 已创建并初始化。

### 2. 安装 Python 依赖
```bash
pip install -r "md txt/requirements.txt"
```

## 二、启动后端服务

### 方式1：直接启动（推荐）
```bash
cd py
python app.py
```

看到以下输出表示启动成功：
```
==================================================
霍格沃茨 MIS 系统启动
运行地址: http://127.0.0.1:5000
调试模式: 开启
==================================================
```

### 方式2：使用 Flask 命令
```bash
cd py
flask run --port=5000
```

## 三、打开前端页面

### 方式1：直接打开HTML文件
双击打开 `frontend/login.html`

### 方式2：使用 Live Server（推荐）
如果使用 VS Code，安装 Live Server 插件，右键 `login.html` 选择 "Open with Live Server"

## 四、测试流程

### 【测试1】注册账号

#### 步骤1：注册教授账号
1. 访问 `login.html`，点击"立即注册"
2. 填写信息：
   - 用户名：`Snape`（至少3个字符）
   - 密码：`123456`（至少6个字符）
   - 确认密码：`123456`
   - 角色：**教授**
   - 学院：无需选择（教授自动跳过）
3. 点击"注册"，成功后自动跳转登录页

#### 步骤2：注册学生账号
1. 再次点击"立即注册"
2. 填写信息：
   - 用户名：`Harry`
   - 密码：`123456`
   - 确认密码：`123456`
   - 角色：**学生**
   - 学院：格兰芬多（house_id=1）
3. 点击"注册"

#### 步骤3：再注册几个学生（可选）
- 用户名：`Draco`，学院：斯莱特林（house_id=2）
- 用户名：`Hermione`，学院：格兰芬多（house_id=1）

---

### 【测试2】教授端工作台测试

#### 步骤1：教授登录
1. 在 `login.html` 输入：
   - 用户名：`Snape`
   - 密码：`123456`
2. 点击"登录"
3. **验证点**：应自动跳转到 `professor.html`（教授工作台）

#### 步骤2：查看学生列表
- **验证点**：页面加载后，"选择学生"下拉框应显示所有学生
- 示例：`Harry (学院ID: 1)`、`Draco (学院ID: 2)`

#### 步骤3：提交积分工单
1. 选择学生：`Harry (学院ID: 1)`
2. 分数变动：`10`（加分）
3. 原因说明：`魔药课表现优秀`
4. 点击"提交工单"
5. **验证点**：
   - 显示"积分工单提交成功！"
   - 表单自动清空
   - 操作历史表格新增一条记录（绿色 `+10`）

#### 步骤4：提交扣分工单
1. 选择学生：`Draco (学院ID: 2)`
2. 分数变动：`-5`（扣分）
3. 原因说明：`违反课堂纪律`
4. 点击"提交工单"
5. **验证点**：操作历史表格新增记录（红色 `-5`）

#### 步骤5：查看操作历史
- **验证点**：表格显示所有操作记录，按时间降序排列
- 包含字段：学生姓名、分数变动（红/绿色）、原因、时间

---

### 【测试3】学生端个人中心测试

#### 步骤1：学生登录
1. 点击"退出登录"
2. 在 `login.html` 输入：
   - 用户名：`Harry`
   - 密码：`123456`
3. 点击"登录"
4. **验证点**：应自动跳转到 `student.html`（学生个人中心）

#### 步骤2：查看个人信息
- **验证点**：
  - 学生姓名：`Harry`
  - 所属学院：显示格兰芬多徽章（红色背景）
  - 学院总分：应显示更新后的分数（例如 10 分）

#### 步骤3：查看积分流水
- **验证点**：
  - 显示教授 `Snape` 给的加分记录
  - 分数显示为绿色 `+10`
  - 原因：`魔药课表现优秀`
  - 时间正确

#### 步骤4：测试其他学生
1. 退出登录，用 `Draco` 登录
2. **验证点**：
   - 学院显示斯莱特林徽章（绿色背景）
   - 积分流水显示 `-5`（红色）

---

### 【测试4】鉴权机制测试

#### 测试1：未登录访问
1. 直接访问 `professor.html`
2. **验证点**：应弹出"请先以教授身份登录"，跳转到 `login.html`

#### 测试2：角色权限校验
1. 用学生账号登录
2. 在浏览器地址栏修改URL为 `professor.html`
3. **验证点**：应弹出"请先以教授身份登录"，跳转到 `login.html`

#### 测试3：Token过期处理
1. 登录后，打开浏览器开发者工具（F12）
2. 在 Console 输入：`localStorage.removeItem('token')`
3. 刷新页面或提交表单
4. **验证点**：应自动跳转到 `login.html`

---

## 五、常见问题排查

### 问题1：无法连接后端
**症状**：前端报错"网络错误，请检查连接"

**排查步骤**：
1. 确认后端已启动（终端显示"霍格沃茨 MIS 系统启动"）
2. 检查后端端口是否为 5000
3. 检查 `frontend/js/api.js` 中的 `API_BASE_URL` 是否正确
   ```javascript
   const API_BASE_URL = 'http://localhost:5000/api';
   ```

### 问题2：401 未授权错误
**症状**：接口返回 `{"code": 401, "msg": "未授权"}`

**排查步骤**：
1. 打开浏览器开发者工具（F12）→ Application → Local Storage
2. 检查是否存在 `token` 项
3. 检查 Network 标签，请求头是否包含：
   ```
   Authorization: Bearer <token值>
   ```

### 问题3：数据库连接失败
**症状**：后端报错 `Can't connect to MySQL server`

**排查步骤**：
1. 检查 `.env` 文件配置：
   ```
   DB_HOST=127.0.0.1
   DB_PORT=3307
   DB_USER=root
   DB_PASSWORD=0609
   DB_NAME=Hogwarts Houses
   ```
2. 确认 MySQL 服务已启动
3. 确认数据库 `Hogwarts Houses` 已创建

### 问题4：CORS 跨域错误
**症状**：浏览器控制台报错 `Access-Control-Allow-Origin`

**解决方案**：
- 后端已配置 `Flask-CORS`，应自动处理跨域
- 如果仍有问题，检查后端是否正确启动

---

## 六、快速测试脚本

### 创建测试数据脚本（可选）
在浏览器控制台（F12 → Console）执行：

```javascript
// 查看当前登录信息
console.log('Token:', localStorage.getItem('token'));
console.log('Role:', localStorage.getItem('role'));
console.log('User ID:', localStorage.getItem('user_id'));
console.log('Username:', localStorage.getItem('username'));
console.log('House ID:', localStorage.getItem('house_id'));

// 清除登录信息
localStorage.clear();
```

---

## 七、测试完成检查清单

- [ ] 教授账号注册成功
- [ ] 学生账号注册成功
- [ ] 教授登录后跳转到 professor.html
- [ ] 学生登录后跳转到 student.html
- [ ] 教授能查看学生列表
- [ ] 教授能提交加分/扣分工单
- [ ] 教授能看到操作历史（红/绿色区分）
- [ ] 学生能看到个人信息和学院徽章
- [ ] 学生能看到积分流水（红/绿色区分）
- [ ] 未登录访问自动跳转登录页
- [ ] 角色权限校验生效（学生不能访问教授页面）
- [ ] Token 失效后自动跳转登录页

---

## 八、测试截图位置

建议在测试过程中截图保存：
- `screenshots/professor-workspace.png` - 教授工作台
- `screenshots/student-profile.png` - 学生个人中心
- `screenshots/login-page.png` - 登录页面
- `screenshots/register-page.png` - 注册页面

测试完成后，所有功能点应全部验证通过！🎉
