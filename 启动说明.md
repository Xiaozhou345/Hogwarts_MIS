# 霍格沃茨 MIS 系统 - 快速启动指南

## 🚀 快速启动

### 启动后端
双击 `start.bat` 文件即可启动后端服务器

### 停止后端
双击 `stop.bat` 文件即可停止后端服务器

## 📋 启动步骤

1. **确保MySQL数据库已启动**
   - 地址：127.0.0.1:3307
   - 数据库：Hogwarts Houses

2. **双击 `start.bat`**
   - 会自动启动Flask后端
   - 看到以下信息表示启动成功：
     ```
     ==================================================
     霍格沃茨 MIS 系统启动
     运行地址: http://127.0.0.1:5000
     调试模式: 开启
     ==================================================
     ```

3. **打开前端页面**
   - 双击 `frontend/index.html`
   - 或在浏览器中打开：`C:\Users\zhenz\Desktop\Love from Hogwarts\frontend\index.html`

## 🔧 常见问题

### 问题1：启动失败，提示缺少模块
**解决方案**：安装依赖包
```powershell
pip install flask flask-cors mysql-connector-python python-dotenv
```

### 问题2：数据库连接失败
**解决方案**：
- 检查MySQL服务是否启动
- 检查 `.env` 文件配置是否正确

### 问题3：端口被占用
**解决方案**：
- 使用 `stop.bat` 停止现有进程
- 或修改 `.env` 文件中的 `PORT=5001`

## 🧪 验证启动成功

在浏览器中访问：
```
http://localhost:5000/api/test_db
```

如果看到以下JSON，说明启动成功：
```json
{
  "code": 200,
  "msg": "数据库连接成功!",
  "data": {"VERSION()": "9.6.0"}
}
```

## 📁 项目结构

```
Love from Hogwarts/
├── start.bat          # 启动脚本
├── stop.bat           # 停止脚本
├── 启动说明.md        # 本文件
├── frontend/          # 前端代码
│   ├── index.html     # 主页面
│   ├── login.html     # 登录页
│   └── ...
├── py/                # 后端代码
│   ├── app.py         # Flask主程序
│   └── ...
└── .env               # 配置文件
```

## 💡 使用提示

- **调试模式**：已开启，修改代码会自动重启
- **查看日志**：启动窗口会显示所有API请求日志
- **停止服务**：关闭启动窗口或使用 `stop.bat`

---

**祝使用愉快！** 🧙✨